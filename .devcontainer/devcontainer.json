// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/docker-outside-of-docker
{
	"name": "GDE_B-ALKFET",
	"dockerFile": "Dockerfile",
	"runArgs": [
		"--name",
		"gde_b-alkfet_devcontainer",
		"--network",
		"balkfet-network"
	],
	"features": {
		"ghcr.io/devcontainers/features/docker-outside-of-docker:1": {
			"version": "latest",
			"enableNonRootDocker": "false",
			"moby": "false"
		},
		"ghcr.io/devcontainers/features/powershell:2": {
			"version": "7.5"
		},
		"ghcr.io/devcontainers-extra/features/angular-cli:2": {},
		"ghcr.io/devcontainers/features/kubectl-helm-minikube:1": {
			"version": "latest",
			"helm": "4.1.1",
			"minikube": "none"
		},
		"ghcr.io/devcontainers/features/azure-cli:1": {}
	},
	// Use this environment variable if you need to bind mount your local source code into a new container.
	"remoteEnv": {
		"LOCAL_WORKSPACE_FOLDER": "${localWorkspaceFolder}"
	},
	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	// "forwardPorts": [],
	"initializeCommand": "docker network inspect balkfet-network -f '{{.Name}}' || docker network create balkfet-network",
	// Use 'postCreateCommand' to run commands after the container is created.
	"postCreateCommand": "npm install --prefix /workspaces/GDE_B-ALKFET/source/WebUI",
	"postAttachCommand": "pwsh -NoLogo -NoProfile -NonInteractive -ExecutionPolicy Bypass -Command \"& '/workspaces/GDE_B-ALKFET/tools/setup_devenv.ps1'; exit\"",
	// Configure tool-specific properties.
	"customizations": {
		"vscode": {
			"extensions": [
				"hediet.vscode-drawio",
				"mhutchie.git-graph",
				"ms-dotnettools.csdevkit",
				"Angular.ng-template",
				"PKief.material-icon-theme",
				"humao.rest-client",
				"mongodb.mongodb-vscode",
				"me-dutour-mathieu.vscode-github-actions",
				"redhat.vscode-yaml"
			]
		}
	}
	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "root"
}